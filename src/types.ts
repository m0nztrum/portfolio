export type navItem = {
    name: string;
    link: string;
    icon?: JSX.Element;
};

export type techIconType = {
    name: string;
    description: string;
    imageUrl: string;
    bgColor: string;
};
